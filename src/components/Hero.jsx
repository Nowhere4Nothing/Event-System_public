function Hero() {
    return (
      <section className="hero">
        <h1>Welcome to Eventual Events</h1>
        <p>Book, host, and manage your events in one place.</p>
        <button className="cta-btn">Browse Events</button>
      </section>
    );
  }
  
  export default Hero;
  